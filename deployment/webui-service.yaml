---
apiVersion: v1
kind: Service
metadata:
  name: webui
  labels:
    name: webui
  namespace: default
spec:
  ports:
    # the port that this service should serve on
  - port: 8080
    targetPort: 8080
  selector:
    name: users
  type: LoadBalancer

